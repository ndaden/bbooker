import React from "react"

const PageTitle = ({title} : {title: string}) => <h2 className="text-2xl font-bold leading-7 sm:truncate sm:text-3xl sm:tracking-tight">{title}</h2>
export default PageTitle
